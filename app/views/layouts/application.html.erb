<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>a|zcams</title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <!-- Le styles -->
    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- Le fav and touch icons -->
    <link href="images/favicon.ico" rel="shortcut icon">
    <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
    <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
    <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
  </head>
  <body>
		<div class="navbar navbar-fixed-top">
			<div class="navbar-inner">
					<div class="container">
					<a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
						<span class="icon-bar">Icon bar</span>
						<span class="icon-bar"></span>
						<span class="icon-bar"></span>
					</a>
					<a class="brand" href="#"><font color=#0000bf>a</font>|<font color=#ffffff>z</font>cams</a>
					<div class="container nav-collapse">
						<% if user_signed_in? %>
							<%= render 'mainmenu' %>
							<%= render 'usermenu' %>
						<% else %>
							<ul class="nav">
								<li><%= link_to "Sign in", new_user_session_path %></li>
							</ul>
						<% end %>
					</div><!--/.nav-collapse -->
				</div>
			</div>
		</div>

		<div class="container">
			<div class="content">
				<div class="row">
					<!-- Devise stuff -->
					<% if notice %>
						<div class="alert alert-warning">
							<a class="close" href="#">×</a>
							<p><%= notice %></p>
						</div>
					<% end %>
					<% if alert %>
						<div class="alert alert-error">
							<a class="close" href="#">×</a>
							<p><%= alert %></p>
						</div>
					<% end %>
					<div class="span9">
						<%= yield %>
					</div>
				  <div class="span1 offset2 pull-right">
					  <a href="#myModal" class="btn primary" data-toggle="modal">
              <%= image_tag "light-bulb.png" %>
            </a>
          </div>

          <div class="modal hide fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
            <div class="modal-header">
              <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
              <h3 id="myModalLabel">A bright idea!</h3>
            </div>
            <div class="modal-body">
              <p>Thank you for contributing to making azcams better. Please jot down
              your thoughts in the box below, and then click Save. Do make sure not to
              include any information about clients, so as to maintain their confidentiality.
            </div>
            <div class="modal-footer">
              <button class="btn" data-dismiss="modal" aria-hidden="true">Close</button>
              <button class="btn btn-primary">Save changes</button>
            </div>
          </div>

					<div class="span3">
						<div class="well sidebar-nav">
							<% if user_signed_in? %>
								<h3>Alerts <small></small></h3>
							  <%= render 'activity' unless flash[:activity].nil? %>
							  <%= render :partial => 'printqueue', :locals => { :printqueue => printqueue } unless printqueue.count == 0 %>
							<% end %>
						</div><!--/.well -->
					</div><!--/span-->
				</div><!--/row-->
			</div><!--/content-->

			<footer>
				<br/>
				<p>&copy; 2012 <a href="http://www.appliedzetetics.co.uk">Applied Zetetics</a></p>
				<!-- Place this tag where you want the +1 button to render. -->
				<div class="g-plusone" data-annotation="inline" data-width="300">

					<!-- Place this tag after the last +1 button tag. -->
					<script type="text/javascript">
						(function() {
							var po = document.createElement('script'); po.type = 'text/javascript'; po.async = true;
							po.src = 'https://apis.google.com/js/plusone.js';
							var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(po, s);
						})();
					</script>
				</div>
			</footer>

		</div> <!-- /container -->

    <!-- Le javascript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
